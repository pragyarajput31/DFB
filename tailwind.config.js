module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Specify paths to all template files
  theme: {
    extend: {}, // Extend default theme if needed
  },
  plugins: [], // Add Tailwind plugins here if required
};
